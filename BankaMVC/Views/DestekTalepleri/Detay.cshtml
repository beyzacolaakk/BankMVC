
@model BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekTaleb

@{
    ViewBag.Title = "Destek Talebi Detayı";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="page-title">
    Support Request Detail
</h2>

<div class="card">
    <div class="card-header">
        <div>Request Details</div>
        <div>
            <span class="badge @(Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Open ? "badge-primary" :
                                Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Process ? "badge-warning" :
                                Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Resolved ? "badge-success" : "badge-danger")">
                @Model.Durum
            </span>
        </div>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    <label class="form-label">
                        Customer ID:
                    </label>
                    <div class="form-control">@Model.MusteriId</div>
                </div>
               
                <div class="form-group">
                    <label class="form-label">
                        Subject:
                    </label>
                    <div class="form-control">@Model.Konu</div>
                </div>
      
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    <label class="form-label">
                        Request Date:
                    </label>
                    <div class="form-control">@Model.TalepTarihi.ToString("dd.MM.yyyy")</div>
                </div>

            </div>
        </div>
        
        <div class="form-group">
            <label class="form-label">Explanation:</label>
            <div class="form-control" style="min-height: 100px;">@Model.Aciklama</div>
        </div>
        
  
        
        <hr />
        
        <form method="post" action="@Url.Action("DurumGuncelle", new { id = Model.Id })">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            Update Status:
                        </label>
                        <select name="durum" class="form-control">
                            <option value="0" selected="@(Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Open)">Open</option>
                            <option value="1" selected="@(Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Process)">Process</option>
                            <option value="2" selected="@(Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Resolved)">Resolved</option>
                            <option value="3" selected="@(Model.Durum == BankaMVC.Models.Somut.BankaYonetimPaneli.Models.DestekDurumu.Closed)">Closed</option>
                        </select>


                    </div>
                </div>
            </div>
            
            <div class="form-group">
                <label class="form-label">
                    Response:
                </label>
                <textarea name="yanit" class="form-control" rows="4">@Model.Yanit</textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Update
            </button>
        </form>
    </div>
</div>

<div class="mt-3">
    <a href="@Url.Action("Index")" class="btn btn-primary">
        <i class="fas fa-arrow-left"></i> Return to List
    </a>
</div>
